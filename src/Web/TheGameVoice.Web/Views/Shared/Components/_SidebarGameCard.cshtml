@model TheGameVoice.Domain.Entities.Game

<a asp-controller="Games"
   asp-action="Details"
   asp-route-slug="@Model.Slug"
   class="group block overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition duration-300 hover:-translate-y-1 hover:shadow-md">


    @* Add game image here later *@
    @*
@if (Model.FeaturedImage != null)
{
    <div class="aspect-video overflow-hidden">

        <img src="@Model.FeaturedImage.FilePath"
             alt="@Model.Name"
             class="h-full w-full object-cover transition duration-500 group-hover:scale-105" />

    </div>
}
*@

    <div class="p-5">

        <div class="mb-2 text-xs font-bold uppercase tracking-[0.15em] text-primary">

            Game

        </div>

        <h3 class="text-lg font-semibold leading-tight text-slate-900 transition group-hover:text-primary">

            @Model.Name

        </h3>

        @if (!string.IsNullOrWhiteSpace(Model.Summary))
        {
            <p class="mt-3 line-clamp-3 text-sm leading-relaxed text-slate-500">

                @Model.Summary

            </p>
        }

    </div>


</a>
