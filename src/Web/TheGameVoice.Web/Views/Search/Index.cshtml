@model TheGameVoice.Web.ViewModels.Search.SearchResultsViewModel

<div class="tgv-container py-fluid-12">

    <!-- SEARCH HERO -->

    <section class="overflow-hidden rounded-3xl border border-line bg-surface shadow-sm">

        <div class="p-8 md:p-12">

            <div class="mb-4">

                <span class="inline-flex items-center rounded-full bg-primary/10 px-4 py-2 text-xs font-bold uppercase tracking-[0.15em] text-primary">

                    Search

                </span>

            </div>

            <h1 class="text-4xl font-bold tracking-tight text-ink md:text-5xl">

                Search TheGameVoice

            </h1>

            <p class="mt-4 max-w-3xl text-lg leading-relaxed text-ink-mid">

                Find gaming news, reviews, impressions, guides and features across TheGameVoice.

            </p>

            <form method="get"
                  asp-controller="Search"
                  asp-action="Index"
                  class="mt-8">

                <div class="flex flex-col gap-3 md:flex-row">

                    <input type="text"
                           name="query"
                           value="@Model.Query"
                           placeholder="Search articles..."
                           class="flex-1 rounded-2xl border border-line bg-surface-muted px-6 py-4 text-base outline-none transition focus:border-primary focus:bg-surface" />

                    <button type="submit"
                            class="rounded-2xl bg-primary px-8 py-4 font-semibold text-white transition hover:opacity-90">

                        Search

                    </button>

                </div>

            </form>

        </div>

    </section>

    @if (!string.IsNullOrWhiteSpace(Model.Query))
    {
        <section class="mt-fluid-12">

            <div class="mb-8 flex flex-wrap items-center justify-between gap-4 border-b border-line pb-4">

                <div class="text-sm text-ink-muted">

                    Found

                    <span class="font-semibold text-ink">

                        @Model.Articles.Count

                    </span>

                    result(s) for

                    <span class="font-semibold text-primary">

                        "@Model.Query"

                    </span>

                </div>

            </div>

            @if (Model.Articles.Any())
            {
                <div class="rounded-3xl border border-line bg-surface p-6 shadow-sm">

                    @foreach (var article in Model.Articles)
                    {
                        <partial name="~/Views/Shared/Components/_ArticleCard.cshtml"
                                 model="article" />
                    }

                </div>
            }
            else
            {
                <div class="rounded-3xl border border-line bg-surface p-fluid-16 text-center shadow-sm">

                    <div class="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-surface-raised text-fluid-3xl">

                        🔍

                    </div>

                    <h2 class="text-fluid-3xl font-bold text-ink">

                        No Results Found

                    </h2>

                    <p class="mt-4 text-ink-muted">

                        We couldn't find any articles matching

                        <span class="font-semibold text-ink-soft">

                            "@Model.Query"

                        </span>

                    </p>

                    <p class="mt-2 text-ink-muted">

                        Try different keywords, broader terms or check your spelling.

                    </p>

                </div>
            }

        </section>
    }
    else
    {
        <section class="mt-fluid-12">

            <div class="rounded-3xl border border-line bg-surface p-fluid-16 text-center shadow-sm">

                <div class="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-surface-raised text-fluid-3xl">

                    🎮

                </div>

                <h2 class="text-fluid-3xl font-bold text-ink">

                    Search Gaming Content

                </h2>

                <p class="mt-4 text-ink-muted">

                    Search for news, reviews, games, opinions and features from TheGameVoice.

                </p>

            </div>

        </section>
    }

</div>